# 專案交接：日文閱讀輔助網頁（Japanese Reading Helper）

## 一句話目標
做一個「手機也能用的網頁」，讓使用者貼上一段日文後，能：標假名、斷詞並標詞性、點單字查中文意思、看整句中日對照翻譯。使用者是日文初學者（JLPT N5 程度、正在背單字文法），主要用途是把日文素材變得「好讀好學」。

---

## 核心功能需求（四項全要）

1. **自動標假名（ふりがな）**
   - 漢字上方標註平假名讀音，像日文教材的 ruby 標註。
   - 使用者漢字還不熟，這是最高優先功能。
   - HTML 用 `<ruby>` / `<rt>` 標籤呈現。

2. **自動斷詞 + 標詞性**
   - 把連續的日文句子切成一個個單字（形態素），並標出詞性（名詞／動詞／助詞／形容詞等）。
   - 詞性建議用顏色或小標籤區分，方便初學者看出句子結構。

3. **點字查中文意思**
   - 點（手機是 tap）任一個單字，顯示該詞的中文解釋、原形（辭書形）、詞性。
   - 例如點「食べました」要能顯示：原形「食べる」、意思「吃」、過去肯定形。

4. **整句翻譯對照**
   - 提供整段的中文翻譯，與日文對照顯示（並排或上下對照）。

---

## 技術方案（已定調）

採**混合方案**，兩種功能用兩種技術，各取所長：

### A. 標假名 / 斷詞 / 詞性 → 用 kuromoji.js（前端、免費、離線）
- **kuromoji.js** 是純 JavaScript 的日文形態素解析器，瀏覽器端可直接跑，不需後端伺服器。
- 負責：斷詞、取得每個詞的讀音（可轉成 ふりがな）、詞性、辭書形（原形）。
- 優點：免費、不需 API 金鑰、離線可用、手機網頁跑得動。
- 注意：kuromoji 字典檔（dict）有一定體積，首次載入需下載字典，要處理載入中的 loading 狀態。

### B. 查中文字義 / 整句翻譯 → 串 Claude API
- 「點詞查中文解釋」與「整句翻譯對照」交給 Claude API（Anthropic），品質最好。
- 詞義查詢：把單字（含上下文）送給 API，回傳簡潔中文解釋。
- 整句翻譯：把整段日文送給 API 翻成中文。
- 建議 model 用當前可用的 Claude Sonnet 系列（呼叫前確認最新 model string）。
- **成本控制**：斷詞標音全部由 kuromoji 在前端完成、不呼叫 API；只有「查意思」和「翻譯」才呼叫 API，降低花費。

### 架構
- **純前端網頁**，建議 React（也可純 HTML+JS）。
- 手機優先（responsive），版面在窄螢幕要好讀好點。
- 部署：GitHub Pages 或 Vercel，手機開網址即可用。

---

## 待處理 / 需要決定的事項（Claude Code 請主動提醒使用者）

1. **API 金鑰安全**：純前端直接放 Anthropic API 金鑰會外洩。正式上線需要一個輕量後端（或 serverless function，如 Vercel Functions）代理 API 呼叫，把金鑰藏在後端。初期本機自用可先簡化，但要明確告知使用者這個風險與正式做法。
2. **kuromoji 字典載入體驗**：首次載入字典較慢，需設計 loading 提示，避免使用者以為當機。
3. **標假名的顯示切換**：建議做一個開關，可切換「顯示／隱藏 假名」，方便使用者自我測驗（先不看假名試著讀）。
4. **詞性顏色對應**：需定義一套詞性→顏色的對照（名詞、動詞、助詞、形容詞、副詞…），色彩不要太多太雜。

---

## 使用者背景（供判斷設計取捨）
- JLPT N5 程度，2025 年 12 月考 N5，明年中目標 N3。
- 漢字量少，所以「標假名」「查中文意思」是剛需。
- 慣用繁體中文；介面文字請用繁體中文，且**不要出現任何簡體字**。
- 偏好乾淨、直接、好操作的介面，不要過度花俏。

## 建議的開發順序（MVP 優先）
1. 先做「貼上日文 → kuromoji 斷詞 + 標假名 + 詞性顏色」這條主線（純前端，最快看到成果、且不花錢）。
2. 再加「點詞顯示原形 / 詞性」（資料 kuromoji 已有，先不串 API，用 kuromoji 的辭書形）。
3. 接著串 Claude API 做「點詞查中文意思」。
4. 最後做「整句翻譯對照」。
5. 之後再處理部署與 API 金鑰後端代理。

---

## 給 Claude Code 的第一步建議
先確認開發環境與使用者想用的框架（React vs 純 HTML），然後從第 1 步 MVP 開始：搭好 kuromoji.js 斷詞 + ふりがな 標註 + 詞性上色的最小可跑版本，讓使用者先在手機上看到日文被斷詞標音的效果，再逐步往上加功能。
